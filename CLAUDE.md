# CLAUDE.md — Novel Studio AI 開発ガイド

このファイルはPhase終了時に必ず更新する。実装を始める前に必ず読むこと。

## アプリ目的

AIを本格的に活用した小説制作専用デスクトップIDE。単なるAI文章生成ツールでは
なく、執筆・原稿用紙・キャラクター・世界観・プロット・時系列・伏線・資料・
AI相談・矛盾チェック・推敲・Exportまでを1つに統合し、「作者が1つのアプリから
離れずに長編小説を完成させられる」ことを目標とする。基本思想は
「AIに全部書かせるアプリ」ではなく「作者を支援するアプリ」。

## 技術構成

- デスクトップ基盤: Tauri 2 (Rust)
- フロントエンド: React + TypeScript + Vite
- 状態管理: Zustand
- 入力検証: Zod
- DB: SQLite (`rusqlite`, bundled — システムのlibsqlite3に依存しない)
- テスト: Vitest (+ Testing Library) / `cargo test` / 将来Playwright (E2E)
- パッケージ管理: pnpm
- 対象プラットフォーム: Windows最優先、将来macOS対応を見据えた設計にする

## コーディング規約

- 巨大な1ファイル・何千行の1コンポーネントを禁止。feature/テーブル/
  ユースケース単位でファイルを分割する（詳細: `docs/ARCHITECTURE.md`）。
- フロントの層: UI → Application Services (`src/services`) → Domain
  (`src/domain`) → (Tauri IPC) → Rust側 Repositories → SQLite。
  featureやコンポーネントから `@tauri-apps/api` を直接importしない。
- Rust側で生SQLを書けるのは `src-tauri/src/repositories/*` のみ。
- AI関連は独立レイヤー（`docs/AI.md`）。他機能からAI Gatewayへの逆依存を作らない。
- アプリ名はハードコードしない。真実源は `src-tauri/tauri.conf.json`
  (`productName`) と `src/app/config.ts` (`APP_NAME_FALLBACK`) の2箇所のみ。

## DB方針

- SQLite、ローカル保存。マイグレーションは `src-tauri/migrations/NNNN_*.sql`
  （連番・追記専用・一度リリースしたら書き換えない）。詳細: `docs/DATABASE.md`。
- IDはUUID。タイトル等をIDにしない。
- 何でもJSON1列に押し込まない。柔軟な拡張項目のみJSON列を許容。
- 重要データはSoft Delete方針（Phase 4以降、該当機能と一緒に導入）。

## テスト方法

```bash
pnpm typecheck   # tsc -b --noEmit
pnpm lint        # eslint
pnpm test        # vitest run
pnpm build       # tsc -b && vite build
cd src-tauri && cargo test   # Rustユニットテスト
cd src-tauri && cargo build  # Tauriアプリのフルビルド
```

Phase終了時は必ず上記すべてを実行し、エラー・失敗テストがない状態で報告する。

## UI原則

落ち着いた高級感のある執筆ソフト（Minimal / Professional / Focused / Modern /
Calm）。過剰なゲーミングUIは禁止。Light/Dark両対応、アクセントカラーは設定変更
可能にする（実装済みの土台: `src/styles/theme.css` のCSS変数）。エディタ領域の
視認性を最優先する。日本語IME入力を最優先でテストする（詳細: `docs/EDITOR.md`）。

## AI機能の原則

- AI機能が利用不能でも通常の小説執筆機能は動作すること。
- AIが本文を書き換える場合、必ずユーザーの承認（差分表示→採用/部分採用/
  再生成/破棄）を挟む。
- 作品データは原則ローカル。AI機能利用時のみ選択データを送信し、Context
  Inspectorで送信内容を事前確認できるようにする。
- APIキーはソースコードに埋め込まない・平文DB保存しない・ログに出さない。
- 詳細: `docs/AI.md`。

## 禁止事項

- 巨大なApp.tsx / 何千行の1コンポーネント
- UIだけのダミー機能（ボタンはあるが中身がない）
- APIキーのハードコード
- 作品全文の不要な外部送信
- DBを毎回作り直す運用
- 原稿をlocalStorageだけで保存する設計
- AIレスポンスで原稿を即上書き
- エラーの握りつぶし
- テストなしでの大規模変更
- 既存機能を勝手に削除
- 「簡単のため」という理由での仕様の大量省略
- 無断での技術構成の全面変更

## 判断に迷った場合の優先順位

```
データ安全性 > 執筆体験 > 安定性 > 操作性 > パフォーマンス > AI機能 > 見た目
```

## 現在のPhase

**Phase 17（AIキーのOS資格情報ストア連携、v0.1.1）— 完了**。計画して
いた全Phase(0〜11)完了後、リリース後の追加要望として着手したPhase12
(作品設計チャット)・Phase13(テーマ拡張 + AI執筆支援の分かりやすさ
改善)・Phase14(作品削除ボタンの不具合修正)・Phase15(Auto Update)・
Phase16(単一ペインセクションのスクロール不具合修正)に続く6件目の
追加対応。Phase5からの技術的負債だった「AIキーがメモリ上にしか保持
されず、アプリ再起動のたびに再入力が必要」という制約を、`keyring`
クレート経由のOS資格情報ストア連携に置き換えて解消した(詳細:
`docs/ROADMAP.md` Phase17)。あわせてv0.1.1としてリリースし、Phase15で
構築したAuto Update機構の実地テスト(既存アプリが新バージョンを検知
できるか)も兼ねる。Semantic Search/Map/Plugin architecture/Local AI/
Cloud sync/Collaboration等は仕様書自身が「初期販売版には必須ではない」
と明示する項目のため、引き続き意図的にスコープ外としている(理由:
`docs/ROADMAP.md` Phase11)。詳細: `docs/ROADMAP.md`。

## 完成済み機能

- Tauri 2 + React + TypeScript + Vite の起動基盤
- SQLite接続・WALモード・マイグレーションランナー（`0001_init`, `0002_phase1_core`）
- テーマ（Light/Dark）のZustand管理 + SQLite永続化の往復動作
- ErrorBoundary（クラッシュ時に非汎用的なフォールバックUIを表示）
- ファイル+stdoutへの構造化ログ（`tauri-plugin-log`）
- Project作成/一覧/編集/削除（Soft Delete）
- Part > Chapter > Scene 階層構造、Navigatorツリー（追加・展開）
- 標準エディタ（textarea、ネイティブUndo/Redo、IME非干渉、オートセーブ
  debounce 800ms、Ctrl+S強制保存、Focus Mode = Ctrl+Shift+F）
- 文字数・400字詰め換算枚数の表示（章/シーン単位・作品合計）
- 作品全体検索（章/シーンのタイトル・本文のLIKE検索）
- 作品設定フォーム（タイトル・作者・ジャンル・あらすじ等）
- 原稿用紙ビュー（`src/features/manuscript/typeset.ts` の純粋関数による組版:
  20×20/20×40/30×40プリセット、縦書き/横書き切替、禁則処理3段階
  厳密/標準/簡易、ルビ`｜base《reading》`、傍点`[#傍点]...[#傍点終わり]`、
  縦中横、ズーム、ページ送り、印刷プレビュー、換算枚数と実レイアウト枚数の
  区別）。本文編集は引き続き標準エディタで行い、原稿用紙はそのビュー
  （docs/MANUSCRIPT.md方針通り、本文データとビューを分離）
- Character（仕様#22相当のほぼ全項目: 基本/外見/内面/話し方/ストーリー/その他）
  のCRUD、タグ付け、簡易人物相関（一覧形式、Node Graph表示は未実装）
- World Bible（ビルトイン20カテゴリ自動作成＋ユーザー追加カテゴリ、項目CRUD、
  タグ付け）、用語辞典、場所（親子階層）のCRUD
- 自由メモ（フォルダ分類、オートセーブ）
- 共通タグ機構（tags/entity_tagsテーブル、Character・World項目で利用）
- ワークスペースのセクション切り替え（原稿/人物/世界観/ストーリー/AI/
  資料・メモ。実装済みのセクションのみ表示——高度AI分析等はPhase7以降）
- プロットボード（カンバン方式、レーン4種を初回自動作成、カードの
  ドラッグ&ドロップでレーン間移動、レーン追加、カード概要編集）
- 時系列（作中の自由記述日付イベントの追加・編集・削除。実世界暦に縛られない
  架空暦を許容）
- 伏線トラッカー（仕様#35の7ステータス: 構想/設置予定/設置済/
  ヒント提示済/回収予定/回収済/破棄、詳細・メモ）
- TODOリスト（完了チェック、件数表示）
- 本文内コメント（標準エディタのtextarea選択範囲から引用テキストと
  位置を取得し、原稿ワークスペースのサイドパネル「コメント」タブに
  紐付けて追加・解決済み切替・削除。IME安全性を優先しcontentEditable化
  はしていない）
- AI Provider Abstraction（Anthropic/OpenAI/Gemini/OpenAI互換の4種、
  `src-tauri/src/ai/provider.rs`が唯一の分岐点）、AI設定画面（Provider/
  Model/API Key/Base URL/Temperature/Max Output）
- Context Builder（作品概要/現在の章/現在のシーン/直前本文/登場人物/
  世界観設定/未回収の伏線/ユーザーの質問を候補ブロックとして収集）と
  Context Inspector（送信前に内容を確認・チェックボックスで取捨選択。
  バイパス経路なし）
- 作品専用AIチャット（質問→内容確認→送信の順を強制。会話履歴の保存・
  クリア）
- AI使用量表示（本日/今月のリクエスト数・入出力トークン数。金額換算は
  意図的に非表示 — 仕様#46）
- AI機能は完全に任意（未設定でも他の全機能が動作し、AI関連コードは
  独立レイヤーで他機能から参照されない）
- AI小説支援機能（Phase6、`src-tauri/src/ai/features.rs` + `commands::ai::
  run_ai_writing_feature`、フロントエンド`AiWritingAssistPanel`）:
  推敲・続き候補・描写追加・会話改善・キャラクター生成・世界観項目生成・
  プロットカード生成・あらすじ生成・タイトル案の9機能。全機能が
  「入力(必要な機能のみ本文選択必須)→Context Inspectorで送信内容確認→
  送信→結果確認(推敲系3機能は差分表示`DiffView`付き)→適用/再生成/
  コピー/破棄」の順を必ず踏み、AIの応答を受け取った時点では原稿・
  Character・World・Plot等のデータは一切変更されない（`docs/AI.md`4章の
  ルールをバックエンドコマンド層でも保証: `run_ai_writing_feature`自体は
  生成テキストを返すのみで既存テーブルへの書き込みを一切行わない）。
  生成結果のパースは`src/features/ai/parseGenerated.ts`が「ラベル: 値」
  形式の行を読み取る単純な実装（プロンプト側で出力形式を指定）
- 高度AI分析(Phase7、`src-tauri/src/ai/analysis.rs` + `commands::analysis::
  run_ai_analysis`、フロントエンド`AiAnalysisPanel`)6種: 矛盾チェック
  (作品全文×登場人物×世界観×時系列)・Character口調チェック(対象
  キャラクターの一人称/語尾/口癖等の設定と本文を比較)・Timeline矛盾
  チェック・設定矛盾チェック・伏線チェック(ステータス別の放置伏線/
  未登録伏線の指摘)・文体講評(ローカル文体統計を添えてAIが講評)。
  Phase5/6と同じContext Inspector経由の確認フローを踏み、原稿等への
  書き込みは一切行わない読み取り専用機能。結果は`ai_analysis_reports`に
  保存され、分析種別ごとに履歴として見返せる(削除も可能)
- 可読性分析(Phase7、`src-tauri/src/readability.rs`、フロントエンド
  `ReadabilityPanel`)はAI機能から独立したローカル集計のみで動作し、
  AI未設定でも常に利用できる: 総文字数・文数・平均/最長文長・100字超の
  長文数・会話文比率(「」内の文字数)・漢字/ひらがな/カタカナ比率・
  一文あたりの読点数を即時計算して表示
- ワークスペースに「分析」セクションタブを追加(可読性/AI分析のサブタブ)
- バージョン履歴(Phase8、`src-tauri/src/repositories/revisions_repository.rs`):
  章/シーンの本文保存のたびに「最後の履歴から5分以上経過かつ内容が
  変化していれば」自動スナップショットを作成(間引きにより肥大化を
  防止、自動分は1章/シーンあたり最新50件を保持)。原稿ワークスペースの
  サイドパネルに「履歴」タブを追加し、いつでも手動スナップショット
  (ラベル付き)を保存でき、各履歴と現在の内容との差分をPhase6の
  `DiffView`で確認してから復元できる。復元時は復元前の状態も自動で
  バックアップされるため、誤って古い内容に戻しても失われない
- Crash Recovery(Phase8): `app_settings`に起動中フラグを持たせ、次回
  起動時に前回が正常終了していなければアプリ上部に一度だけ案内バナーを
  表示。原稿自体は保存のたびにSQLiteへ同期書き込みされているため
  (800msデバウンスのオートセーブ、Phase1から一貫した設計)、実際に
  失われうるのは直近の保存以降の入力のみ
- Auto Backup(Phase8、`src-tauri/src/db/backup.rs`): `VACUUM INTO`で
  DBファイル全体の一貫したスナップショットを作成(WAL中の書き込みが
  混ざって壊れたバックアップになる心配がない)。アプリ起動のたびに
  自動作成し、最新10件のみ保持。作品一覧画面から手動バックアップ・
  バックアップからの復元(現在の接続を安全に閉じてファイルを置き換え、
  再接続後にマイグレーションを再実行)ができる
- ゴミ箱(Phase8、`src-tauri/src/repositories/trash_repository.rs`):
  Phase1〜4で既に大半のエンティティに導入済みだったSoft Delete
  (`deleted_at`列)を、章・シーン・人物・世界観項目・場所・用語・メモ・
  プロットカード・時系列イベント・伏線・TODOの12種にわたって横断的に
  一覧・復元・完全削除できるようにした(新しい削除方式の追加ではなく、
  既存の仕組みを可視化しただけ)。ワークスペースに「ゴミ箱」セクション
  タブを追加
- Export(Phase9、`src-tauri/src/export/`)6形式: TXT / Markdown / HTML /
  DOCX(Word) / PDF(簡易・横書き) / EPUB。全フォーマットは同じ
  `gather()`(作品→章→シーンの木構造取得、空の本文は読み飛ばす)から
  出発し、フォーマットごとのbuilderが`ExportOutput{filename, mime_type,
  bytes}`という統一形式で返す(テキスト・バイナリを問わずバイト列に
  統一することで、フロント側は`Blob`生成→ダウンロードという1経路だけで
  全形式を扱える)。DOCX(OOXML WordprocessingML)とEPUB(EPUB3)は専用
  クレート(`docx-rs`/`epub-builder`等)を使わず、必要最小限のXML/XHTMLを
  直接組み立てて`zip`クレートでZIPコンテナに詰めている。PDFは`printpdf`
  クレートを使い、IPAゴシックフォント(IPAフォントライセンスv1.0、
  `src-tauri/assets/fonts/`に同梱)をコンパイル時に埋め込むことで日本語を
  正しく表示する(PDF標準14フォントはLatin専用のため)。ワークスペースに
  「エクスポート」セクションタブを追加。**「原稿用紙PDF」(縦書き・
  禁則処理・ルビ・傍点込みの本組版)は独立実装せず**、Phase2から既にある
  原稿用紙ビューの印刷プレビュー(`window.print()`→「PDFとして保存」)を
  引き続き使う方針(理由: `docs/EXPORT.md`4章、安定性優先の判断)
- 製品化(Phase10): Onboarding(`src/features/onboarding/OnboardingModal.
  tsx`、`app_settings`の"app.onboarding_seen"で初回のみ表示、いつでも
  スキップ可能) / Sample Project(`src-tauri/src/sample_project.rs`、
  `create_sample_project`コマンド、作品一覧の「サンプル作品を開く」
  ボタン。`is_sample`フラグ自体はPhase1から存在していたが実際に生成する
  経路が無かった。人物2名・世界観2件・場所1件・用語1件・章1(シーン2、
  実際の日本語の文章入り)・プロットカード2件・時系列イベント1件・
  伏線1件・TODO2件がひと通り揃った、すぐ全機能を試せる実データ) /
  「このアプリについて」画面(`src/features/about/AboutModal.tsx`、
  概要・更新履歴・ライセンス・プライバシーの4タブ。CHANGELOG.md/
  THIRD_PARTY_NOTICES.md/PRIVACY.mdをリポジトリ直下の単一の真実源として
  `include_str!`でそのまま埋め込み、アプリ内に別コピーを持たない) /
  診断ログの書き出し(`export_diagnostics`コマンド、ログフォルダを
  ZIPにまとめてダウンロードさせるのみで自動送信は一切行わない -- 詳細は
  `PRIVACY.md`) / Release build(`cargo tauri build --bundles deb`で
  Linux `.deb`バンドルの生成をこのサンドボックスで実機確認済み。
  パッケージング設定`tauri.conf.json`自体が機能することの確認が目的で、
  配布対象であるWindows向けインストーラの生成はWindows環境が必要な
  ため未検証)
- Command Palette(Phase11、`src/features/commandPalette/CommandPalette.
  tsx`): Ctrl+K(macOSはCmd+K)またはトップバーの「コマンド(Ctrl+K)」
  ボタンでどこからでも開き、作品一覧へ移動・サンプル作品を開く・テーマ
  切替・「このアプリについて」を開く、といったグローバル操作に加え、
  作品を開いている場合は各ワークスペースセクション(原稿/人物/世界観/
  ストーリー/AI/分析/資料・メモ/ゴミ箱/エクスポート)へのジャンプと、
  章タイトルでの検索→該当章の原稿を直接開く、が行える。入力欄への
  文字列フィルタのみの単純な部分一致検索で、あいまい検索(fuzzy match)
  ではない。既存機能への新しい到達手段を追加するのみで、新しいデータや
  機能そのものは増やしていない。この機能を実現するため、ワークスペース
  のセクション状態(`ProjectWorkspacePage`のローカルuseState)を
  `src/stores/workspaceUiStore.ts`(Zustand)へ引き上げ、`AppRouter`が
  ルート定義のみを持つ形にリファクタリングして`HashRouter`をアプリ
  シェル全体(トップバー・モーダル群を含む)の外側で1つだけラップする
  ようにした(Command PaletteがRoute要素の外からでも`useNavigate`を
  使えるようにするため)
- 作品設計チャット(Phase12、`src/features/ai/ConceptChatPanel.tsx`、AI
  セクションの新サブタブ「作品設計チャット」): 作品のアイデアを自由記述
  で入力すると、AIがタイトル案・あらすじ・キャラクター(複数)・世界観
  項目(複数)・プロットカード(複数)を一括で提案する。既存のAI小説支援
  機能(Phase6)と同じ「入力→Context Inspectorで送信内容確認→送信→
  結果確認→適用」の流れを踏み、提案受領時点では何も確定しない。結果は
  種別ごとにチェックボックス付きで表示され、選んだ項目だけが既存の
  Character/World項目/プロットカード作成コマンドを通じて作成される。
  バックエンドは`ai::features`に機能ID`generate_concept`を追加した
  のみで、新規Tauriコマンドは追加していない(既存の`run_ai_writing_
  feature`を、`==TITLE==`等の見出しで区切った構造化テキストを返す
  プロンプト設計によってそのまま流用)。パース処理
  (`src/features/ai/parseGenerated.ts`の`parseConceptDraft`)は
  見出しごとに正規表現で独立して切り出すため、AIの出力で一部の見出しが
  崩れても他のセクションの読み取りには影響しない
- テーマ拡張(Phase13、`src/stores/themeStore.ts` +
  `src/styles/theme.css`): ライト/ダークの2択に加え「かわいい系」
  「かっこいい系」「パンク系」の3テーマを追加し、計5種類から選べる
  ようにした。トップバーの☀/☾トグルボタンをテーマ選択セレクト
  (`.theme-select`)に置き換え。テーマごとに変えるのは色(CSS変数)のみで
  フォントは全テーマ共通のまま(原稿用紙の組版・IME表示に影響する変更を
  避けるため)
- AI執筆支援の分かりやすさ改善(Phase13、`AiWritingAssistPanel` +
  新規`src/features/ai/AiFlowSteps.tsx`): 「どの機能が何をするか
  分かりにくい」「操作の流れが分かりにくい」というフィードバックを受け、
  (1)機能選択プルダウンを本文編集/アイデア生成の2グループに分けて選択中
  機能の説明文を常に表示、(2)「入力→送信内容の確認→結果の確認・適用」
  という3段階の操作フローを常に画面上に表示、の2点を改善した。Phase12の
  `ConceptChatPanel`にも同じフロー表示を追加し、AI機能全体で一貫した
  見せ方にしている
- 作品一覧の削除ボタン(Phase14、バグ修正、
  `src/features/projects/ProjectListPage.tsx`): Phase1の時点で
  バックエンド(`delete_project`コマンド、Soft Delete)とストア
  (`useProjectListStore.remove`)は実装済みだったが、作品一覧画面に
  それを呼び出すボタンが一つも存在せず、UIからは作品を削除する手段が
  無かった(ユーザー報告により発覚)。各作品の行に「削除」ボタンを追加し、
  確認ダイアログ(削除済み作品を作品一覧画面から元に戻す機能はまだ無い
  旨を明示)を経て`remove`を呼ぶようにした。新しいコマンドやDB変更は
  不要だった(既存の未配線の経路をUIに繋いだだけ)
- Auto Update(Phase15、`tauri-plugin-updater` + `tauri-plugin-process`、
  `src/services/updateService.ts` + `src/features/update/
  UpdateBanner.tsx`): 起動のたびに一度だけバックグラウンドで新
  バージョンの有無を確認し、見つかった場合のみ画面上部にCrash
  Recovery(Phase8)と同じ見た目の案内を表示する。ダウンロード・適用・
  再起動は必ずバナーの「今すぐ更新」ボタンを押した時だけ実行され、
  バックグラウンドで勝手に更新が当たることはない(執筆中に無断で
  再起動されないようにするため)。「このアプリについて」→「概要」
  タブからいつでも手動確認もできる。配信先はGitHub Releases
  (`latest.json`)、`.github/workflows/release.yml`でタグpush時の
  ビルド・署名・Release下書き作成を自動化した。署名鍵(Ed25519)は
  このセッションで生成し公開鍵のみリポジトリに含め、秘密鍵はユーザー
  本人にのみ別途受け渡した。詳細・既知の制約: `docs/UPDATE_RELEASE.md`
- 単一ペインセクションのスクロール不具合修正(Phase16、
  `src/styles/global.css`の`.workspace__body--single`): Character/
  World/Story/AI/分析/資料・メモ/ゴミ箱/エクスポートの全セクションに
  共通する親コンテナに`overflow-y: auto`が無く、内部で独自のスクロール
  領域を持たないパネル(エクスポート・ゴミ箱・人物相関タブ等)で、
  画面の高さを超えた内容がスクロールバーも無いまま下端で見切れて
  しまっていた(ユーザー報告により発覚)。共通の親コンテナ1箇所の修正で
  横断的に解消した(個々のパネルの修正は不要だった)
- AIキーのOS資格情報ストア連携(Phase17、v0.1.1、
  `src-tauri/src/ai/keystore.rs`): `keyring`クレート経由でWindows
  Credential Manager / macOS Keychain / Linux Secret Serviceへ保存する
  ようにし、Phase5からの技術的負債(アプリ再起動のたびにAPIキーの
  再入力が必要)を解消した。自前の暗号化コードは書かず、各OSの既存の
  安全な仕組みにそのまま委ねる。資格情報ストアが利用できない環境では
  失敗をログに警告として記録した上でセッション中のみメモリで動作を
  継続し、AI機能・アプリ自体は止めない。フロントエンドから見た挙動
  (APIキーの値がフロントへ返らないこと)は変更なし
- Vitest 40件（Phase3から31件で据え置きだったが、Phase12で
  `parseConceptDraft`等のパース処理を検証する5件、Phase13でテーマ拡張の
  4件を新規追加。Phase14・Phase16はUI/CSS配線のみの修正のため件数据え
  置き） / `cargo test` 54件（Phase8で9件追加、Phase9で3件追加、
  Phase10で1件追加、Phase12で1件追加: generate_conceptのプロンプトが
  全見出しを含むことの確認、Phase17で1件追加: `ai::keystore`でプロバ
  イダー間でキーが混ざらないことの確認。Phase13・Phase14・Phase15・
  Phase16はRust側のテスト対象の変更なしのため件数据え置き。
  sample_project::create が人物・世界観・原稿・プロット・時系列・伏線・
  TODOをすべて正しく生成することの確認は引き続きグリーン）、いずれも
  グリーン

## 未完成機能

計画していたPhase(0〜11)はすべて完了した。以下は仕様書自身が「初期
販売版には必須ではない」と明示する高度機能であり、意図的にスコープ外
としている(理由の詳細は`docs/ROADMAP.md`Phase11):

- **Semantic Search**: 埋め込みベクトル検索基盤(ベクトルDB、埋め込み
  生成パイプライン)が必要で、現在のSQLite単体構成から大きく外れる
- **Map(地図機能)**: 仕様#29相当。専用の描画・編集UIが必要な独立機能
- **Custom templates / Plugin architecture**: 外部コードを読み込む
  仕組みが必要で、データ安全性・セキュリティ上の検討が別途必要
- **Local AI(オンデバイスLLM)**: 現在のAI Provider Abstractionは
  外部API前提の設計。オンデバイス推論はモデル配布・実行基盤が別途必要
- **Cloud sync / Multi-device / Collaboration**: サーバー基盤・
  同期プロトコル・権限モデルが必要で、「作品データは原則ローカル」
  という現在の中核方針(CLAUDE.md)からの大きな転換を伴う

Command Palette(Phase11)自体は以下を意図的に見送っている:

- **あいまい検索(fuzzy match)非対応**: 単純な部分一致(大文字小文字を
  無視した文字列包含)のみ。タイプミストレラントな検索は無い
- **シーン単位のジャンプなし**: 章タイトルでの検索・ジャンプのみ対応
  (章は常に`chapters`ストアにロード済みだが、シーンは章を展開するまで
  遅延ロードされる設計のため、シーンまでジャンプ対象に含めると
  Command Palette自体が追加のデータ取得を必要とし複雑化する。章を開けば
  Navigatorから該当シーンへは1クリックで到達できる)
- **キーボードでの上下移動選択なし**: Enterで先頭候補を実行できるが、
  矢印キーでの候補選択・ハイライト移動は未実装(マウスクリックまたは
  絞り込んでEnter、という操作のみ)

Phase 10内でも以下は意図的に見送っている:

- **Windows/macOS向けインストーラの生成・署名は未検証**: `cargo tauri
  build --bundles deb`でLinux `.deb`バンドルの生成には成功し、
  `tauri.conf.json`のパッケージング設定自体が機能することは確認できたが、
  配布対象であるWindows向け(`.msi`/`.exe`、NSIS/WiX)の生成、および
  コード署名は、当然ながらWindows環境(実機またはCI)でなければ検証
  できない
- **Onboardingは固定4ステップのみ**: 対話的なチュートリアル(実際に
  クリックさせて機能を覚えさせる形式)ではなく、静的なテキストによる
  簡潔な案内に留めている。「いつでもスキップできる」ことを「機能を
  誤解なく覚えさせる」ことより優先した判断(CLAUDE.md優先順位: 執筆体験)
- **サンプル作品は1種類のみ**: ジャンル別(ミステリー/ライトノベル等)の
  複数サンプルや、サンプルを最新版に追従して更新する仕組みは無い
- **診断ログの書き出しは全ログファイルを対象**: 個人情報が原稿本文に
  含まれログに混入しないことは実装上保証している(本文・APIキーは
  一切ログ出力しない)が、期間指定・フィルタリング等の絞り込みUIは無く、
  ログフォルダの中身をまるごとZIPにするのみ

Phase 9内でも以下は意図的に見送っている:

- **原稿用紙PDF(縦書き・禁則処理・ルビ・傍点込みの本組版)の独立実装なし**:
  `docs/EXPORT.md`4章の判断により、原稿用紙ビューの印刷プレビュー
  (`window.print()`→「PDFとして保存」)に委ねている。Exportパネルには
  その旨の案内文のみ表示
- **PDFの改行判定は近似**: 正確なグリフ幅測定ではなく「1文字=1em」の
  近似でページ幅から1行あたりの文字数を概算している(和文フォントは
  ほぼ正方形のため実用上十分だが、欧文・半角文字が混ざると誤差が出る)
- **DOCX/EPUBの段落変換は簡易的**: 改行のみを段落境界として扱う単純な
  変換で、原稿用紙専用記法(ルビ`｜base《reading》`・傍点
  `[#傍点]...[#傍点終わり]`)はそのままの文字列として出力される
  (変換・除去は行わない)
- **Export履歴・再ダウンロード機能なし**: 書き出しは毎回その場で生成する
  スナップショットのみで、過去に書き出したファイルの一覧や管理機能は無い

Phase 8内でも以下は意図的に見送っている:

- **プロジェクト単位のゴミ箱UIなし**: 章/シーン/人物等の項目単位の
  ゴミ箱は実装済み。作品一覧からの削除自体はPhase14で作品一覧画面に
  「削除」ボタンを追加して使えるようにしたが(下記既知の問題参照)、
  削除済みプロジェクトを一覧・復元するUIはまだ無い(作品一覧画面は
  引き続き`deleted_at IS NULL`のみ表示)
- **バックアップの世代管理は固定10件**: 世代数の設定変更UIや、
  「重要な節目で手動バックアップを残しつつ自動分だけ間引く」といった
  高度な保持ポリシーは無い
- **Revision同士の相互比較なし**: 差分表示は常に「現在の内容 vs
  選択した履歴」のみで、任意の2つの履歴同士を比較する機能は無い
- **Crash Recoveryは案内のみ**: 実際のデータはPhase1からのオートセーブ
  設計により既に安全に保存されているため、案内バナー以上の特別な
  復旧処理(未保存差分の自動マージ等)は行わない(そもそも
  デバウンス保存のため「未保存差分」という概念がほぼ存在しない設計)

Phase 7内でも以下は意図的に見送っている:

- **長編作品での文字数超過対策**: 高度AI分析は「作品全文」を候補ブロック
  としてそのまま提示する(Context Inspectorで送るかどうかはユーザーが
  選べるが、要約・章単位分割・チャンク送信などの自動的な対策はまだ無い)。
  長編になるほどプロバイダーのトークン上限に達する恐れがあり、既知の
  制約として記録する
- **分析結果から本文該当箇所へのジャンプ導線なし**: 矛盾チェック等の
  指摘はテキストとして提示するのみで、指摘箇所の原稿へ自動でジャンプ
  する機能は無い(結果を見てユーザーが手動で該当章/シーンを開く運用)
- **伏線チェック/設定矛盾チェック等の自動実行・定期実行はなし**:
  すべてユーザーが明示的に「送信内容を確認」→「この内容で分析」と
  操作した時のみ実行される(バックグラウンドでの自動監視は行わない、
  データ安全性・API費用の観点からの意図的な判断)

Phase 6内でも以下は意図的に見送っている:

- **Chapter構成の提案機能**（章立てそのものをAIに生成させる機能）は
  仕様上の優先度を下げ、Phase11（高度機能）以降で改めて検討する
- **プロット生成の対象レーン選択**: `generate_plot`はカードを常に
  先頭レーン（通常「構想」）へ追加する。レーンを選んで生成する導線は
  未実装（作成後にPlot Boardで手動移動は可能）
- **タイトル案の直接適用なし**: `generate_titles`のみ「適用」ボタンを
  出さずコピーのみとしている（作品タイトルは1つしかなく、どの案を
  採用するかは必ず人間が選ぶべき判断のため、意図的に直接反映経路を
  作らなかった）
- **生成結果パースの限界**: `parseGenerated.ts`は「ラベル: 値」形式の
  行を単純に読み取るのみで、AIの出力がこの形式から外れた場合は該当
  フィールドが空になる（フォーム作成後にユーザーが手動編集する前提。
  パース失敗時にエラー表示は出すが自動補正はしない）

Phase 5内でも以下は意図的に見送っている（詳細は`docs/AI.md` 8.1章）:

- **HTTP通信の実装方式**: この開発環境からcrates.ioへの新規クレート
  取得がネットワーク障害でできなかったため、`ureq`/`reqwest`ではなく
  OS標準搭載の`curl`をサブプロセス起動する暫定実装
  （`src-tauri/src/ai/http_client.rs`）。ネットワーク制約解消後に
  専用クレートへの置き換えを検討する技術的負債として記録する
- **APIキーの永続化**: 当時`keyring`クレート(OS資格情報ストア連携)が
  未導入のため、ディスクへ一切保存せずアプリ実行中のみメモリ上に保持
  する方式にしていた。**Phase17でOS資格情報ストア連携へ置き換え済み**
  （`src-tauri/src/ai/keystore.rs`。詳細: `docs/ROADMAP.md` Phase17）
- 実プロバイダー（Anthropic/OpenAI/Gemini等）との疎通は本サンドボックス
  の既知の制約（後述）により未検証
- Rewrite preview・Diff表示UI（Phase6の推敲機能等とセットで実装予定）

Phase 4内でも以下は意図的に見送っている:

- プロットボードのレーン並べ替え・リネームUI（リポジトリ関数
  `rename_plot_lane`/`delete_plot_lane`はコマンドまで実装済みだが、
  UIからの呼び出し導線は未実装。レーン追加のみUIあり）
- 時系列イベントのドラッグ&ドロップ並べ替え（Navigatorと同様、Phase11の
  D&D整備とあわせて検討）
- コメントの返信・スレッド化（現状は単一コメントの追加/解決/削除のみ）
- コメントの本文内ハイライト表示（アンカー位置・引用文は保存しているが、
  textarea上に視覚的なマーカーを重ねる表示はまだ無い。IME安全性を優先し
  contentEditable化を避けた結果のトレードオフ）

Phase 3内でも以下は意図的に見送っている:

- 人物相関図のNode Graph表示（現状は一覧形式。Phase11候補）
- 地図機能（仕様#29、Phase後半）
- 本文中の用語クリック参照（Phase5でAI Context Builderは実装したが、
  本文中の用語ハイライト・クリック参照UI自体は未着手のまま。Phase6以降
  で改めて検討）
- World項目/場所の画像添付の実ファイル保存フロー（`image_path`列は用意
  済みだが、ファイル選択・保存UIは未実装）
- Character/World項目UI自体のフロントエンドテスト（バックエンドの
  repositoryテストのみ。CRUDが薄いラッパーのため、実装より先に手厚い
  UIテストを書くコストとのバランスを取った判断）

Phase 1〜2内でも以下は意図的に見送っている
（ダミーUIを避けるため、「動く最小限」を優先した判断 — 理由は各コンポーネント
のコメント参照）:

- リッチテキスト整形（見出し・強調）、本文内コメント → 通常エディタは
  引き続きplain textarea。ルビ・傍点は原稿用紙ビュー側の専用インライン記法
  (`｜base《reading》` / `[#傍点]...[#傍点終わり]`) として実装済みだが、
  標準エディタ上でボタン操作から挿入するUIはまだ無い（現状は記法を
  直接入力する必要がある。仕様#15「記号を見せない」の理想には未達）
- Navigatorのドラッグ&ドロップ並べ替え → 現在は追加のみ
- 原稿用紙PDF出力・DOCX/EPUB等のExport → Phase 9
- 原稿用紙ビューは表示専用（セル上での直接編集はできない） →
  IME安全性を最優先した設計判断（docs/MANUSCRIPT.md参照）。将来的に
  セル位置⇔本文位置の双方向マッピングが安全に実装できると判断できたら
  再検討する

## 既知の問題

- 本開発セッションはLinuxクラウドサンドボックス内で行っており、Windows実機
  でのビルド・起動・IME動作検証はまだ行えていない。`cargo build` によるLinux
  向けデバッグビルドと、Xvfb上での起動・SQLite読み書き・マイグレーション
  アップグレード（0001→0002、既存データ保持）確認は完了している。
  Windows向けの正式な動作確認は、Windows環境（実機またはCI）で別途行う必要が
  ある。特にMicrosoft IME実機テスト（仕様#94）は最優先で行うこと。
- リリース/バンドル（`tauri build` によるインストーラ生成）はまだ試していない。
  Phase 10（製品化）で本格対応する。
- E2Eテスト（Playwright等）は未導入。Phase 2以降、操作フローが増えた時点で
  導入する。
- 作品全体検索はPhase1時点でLIKE検索のみ。データ量が増えた場合の性能は
  未検証（docs/DATABASE.md 6章の方針通り、必要になった時点でFTS5等を検討）。
- 原稿用紙ビューの禁則処理は簡易実装（行頭禁則・行末禁則の基本ルールのみ、
  複数文字にまたがるぶら下げの高度な調整は未対応）。縦中横は2桁数字等の
  幅を詰める本格的な組み方ではなく、CSS `text-combine-upright` による
  表示上の結合のみ（セル幅計算は1文字1マスのまま）。
- 印刷プレビューは `window.print()` によるブラウザネイティブ印刷に依存。
  Tauri WebView上での挙動はLinux実機（Xvfb、印刷ダイアログ自体は未検証）
  でのみ確認。Windows WebView2上での動作は未検証。
- 【Phase5】AI Provider呼び出しのHTTP通信は、本来`ureq`/`reqwest`等の
  専用クレートを使う設計にしたかったが、この開発環境からcrates.ioへの
  新規クレート取得がネットワーク障害（index.crates.ioへの新規パッケージ
  問い合わせが継続的にタイムアウト）で行えなかったため、OS標準搭載の
  `curl`をサブプロセス起動する暫定実装にした。ネットワーク制約が解消
  され次第、専用クレートへの置き換えを最優先で検討すること。詳細:
  `docs/AI.md` 8.1章。
- 【Phase5→Phase17で解消】APIキーは当初`keyring`クレートが未導入のため
  メモリ上にのみ保持していたが、Phase17でOS資格情報ストア連携に
  置き換え済み。下記Phase17の項目を参照。
- 【Phase5】実際のAIプロバイダー(Anthropic/OpenAI/Gemini等)とのAPI疎通
  は、本サンドボックスのネットワーク許可リストの都合上コード実装のみで
  未検証(Windows実機検証と同様の既知の制約)。実運用前に実際のAPIキーで
  の疎通確認が必要。
- 【Phase6】AI小説支援機能(推敲/続き候補等9機能)も同じ理由で実プロバイダー
  との疎通は未検証。プロンプト内容(`src-tauri/src/ai/features.rs`)と
  レスポンスのパース処理(`parseGenerated.ts`)はユニットテストのみで
  検証しており、実際のLLM出力の揺れ(ラベル形式の崩れ等)に対する
  頑健性は実運用開始後に要観察。
- 【Phase7】高度AI分析(矛盾チェック等6種)も同じ理由で実プロバイダーとの
  疎通は未検証。特に「矛盾チェック」等は作品全文を候補ブロックに含める
  ため、長編作品では実際にAPIのトークン上限へ抵触する可能性が高い
  (未検証・既知の制約)。実運用前に、実際の長編データでの疎通確認と、
  必要であれば要約送信・分割送信の実装を検討すること。
- 【Phase8】Crash Recoveryの「正常終了マーカー」は`tauri::RunEvent::Exit`
  で下ろす実装にしているが、本サンドボックスにはGTKが無くウィンドウを
  実際に開いて正常終了させる検証ができない(`cargo build`によるビルド
  確認と、`xvfb-run`上でプロセスを強制終了させた場合にマーカーが
  正しく「1」のまま残ることは確認済み。Windows実機でウィンドウを
  閉じた際にマーカーが正しく「0」に戻ることは別途確認が必要)。
- 【Phase8】Auto Backupは`VACUUM INTO`をSQL文字列に埋め込む方式のため、
  バックアップ先ディレクトリのパスにシングルクォートが含まれる場合の
  エスケープ処理を入れてはいるが、Windows実機の実際のユーザー名・
  パス構成での動作は未検証。またバックアップ/復元とも大きなDB
  ファイルでの所要時間は未計測(長編・長期利用時のパフォーマンスは
  Phase9以降で実データに近い規模でのプロファイリングが必要)。
- 【Phase9】PDF出力(`export::pdf`)は`cargo test`でバイト列が非空である
  ことと`cargo build`でのAPI整合性(型・引数)は確認しているが、実際に
  生成したPDFをPDFビューアで開いて日本語(IPAゴシック埋め込み)が正しく
  表示されるかの目視確認は本サンドボックスでは行えていない(GUI/
  ビューアが無いため)。実運用前に実際のPDFビューアでの目視確認が必要。
- 【Phase9】DOCX/EPUBも同様に、ZIPコンテナとしての構造(`cargo test`で
  非空バイト列であることのみ確認)は検証済みだが、実際にMicrosoft Word /
  主要EPUBリーダー(Apple Books、Kindle等)で正しく開けるかの実機確認は
  未検証(OOXML/EPUB3の直接組み立てのため、仕様に厳密でない箇所があると
  特定のアプリでのみ開けない可能性がある)。
- 【Phase9】crates.ioへのネットワークアクセスがPhase5当時の障害から
  回復していることを確認し、`zip`・`printpdf`クレートを新規追加した。
  Phase5で暫定実装した`curl`サブプロセスHTTPクライアント(`ai::
  http_client`)とメモリのみのAPIキーストア(`ai::keystore`)は、この
  ネットワーク回復を受けて本来なら`reqwest`/`keyring`等の専用クレートへ
  置き換える好機だが、Phase9のスコープ外として今回は着手していない
  (技術的負債として引き続き記録。次のセキュリティ/品質改善タイミングで
  最優先に検討すること)。
- 【Phase10】`cargo tauri build --bundles deb`でLinux `.deb`バンドルの
  生成そのものには成功し(`tauri.conf.json`のパッケージング設定が機能
  することは確認済み)、`target/release/bundle/deb/`に実際に
  インストール可能なファイルが生成されることまでは確認したが、実際に
  そのdebパッケージをインストールして起動する実機テストや、配布対象で
  あるWindows向け(`.msi`)の生成・署名はこのサンドボックスでは行えない
  (GUIが無い、Windows環境が無いため)。
- 【Phase10】Onboardingで案内している内容(Sample Project・ワークスペース
  構成・データ保護機構)は、実際にその通りに動作することを`cargo test`/
  `pnpm test`と本セッションのXvfb実機起動で個別には確認しているが、
  Onboarding画面自体の表示・スキップ操作をGUI上で目視確認することは
  本サンドボックスでは行えていない(App.tsxのロジック・条件分岐は
  コードレビューレベルでの確認に留まる)。
- 【Phase12】作品設計チャット(`generate_concept`)も他のAI機能と同じ
  理由で実プロバイダーからの応答での動作は未検証。プロンプト側で
  `==TITLE==`等の見出し形式を厳密に指定しているが、実際のLLMの出力が
  この形式から崩れた場合(見出し漏れ・順序違い等)にどの程度パースが
  頑健に働くかは、`parseConceptDraft`のユニットテスト(セクション欠落
  パターンを含む)でのみ検証しており、実運用開始後に要観察。
- 【Phase11】Command Paletteは`pnpm typecheck`/`pnpm lint`/`pnpm build`
  が通ること、および`HashRouter`の位置変更後もXvfb実機起動でアプリが
  正常に立ち上がることは確認したが、実際にCtrl+K/Cmd+Kでパレットが
  開閉すること、章検索・セクションジャンプが画面上で正しく機能すること
  自体のGUI上での目視確認は本サンドボックスでは行えていない(キーボード
  イベントハンドリングとルーティングの実装はコードレビューレベルでの
  確認に留まる。Windows実機での動作確認が必要)。
- 【Phase15】Auto Updateは、プラグインの組み込み・`cargo build`・
  Xvfb実機起動・署名鍵の生成に加え、2026-09-19にユーザー実機で
  `v0.1.0`タグのリリース(`.github/workflows/release.yml`によるビルド・
  署名・GitHub Release公開)まで実施・成功を確認した(詳細・
  つまずいた点: `docs/UPDATE_RELEASE.md` 5章)。`tauri.conf.json`の
  `plugins.updater.endpoints`は`https://github.com/satoyu100match-star/
  novel-studio-ai/releases/latest/download/latest.json`で稼働中。
  残る既知の制約は、既存インストール済みアプリが実際に新バージョンを
  検知してダウンロード・適用・再起動できることの確認のみで、これは
  次にバージョンを上げてリリースした時点(例: v0.1.1)で行う(現時点
  では比較対象の新バージョンがまだ存在しないため)。
- 【Phase16】単一ペインセクションのスクロール修正は`pnpm typecheck`/
  `pnpm lint`/`pnpm test`/`pnpm build`が通ること、`cargo build`、
  Xvfb実機起動が正常なことは確認したが、CSSのスクロール挙動自体の
  目視確認はブラウザ/GUIビューアが無い本サンドボックスでは行えて
  いない(Windows実機での確認が必要)。
- 【Phase17】AIキーのOS資格情報ストア連携は`cargo build`/`cargo test`
  (54件、プロバイダー間の分離を含む)、`pnpm typecheck`/`pnpm lint`/
  `pnpm test`/`pnpm build`、Xvfb実機起動まではこのサンドボックスで
  確認済みだが、実際のOS資格情報ストア(Windows Credential Manager)
  との往復動作(「APIキーを設定→アプリを再起動→再入力なしで使える」
  こと)はクラウドサンドボックスからは検証できない。Windows実機での
  確認が必要。あわせてv0.1.1としてのリリース(Auto Updateの実地
  テスト: 稼働中のv0.1.0アプリが新バージョンを検知できるか)も
  ユーザー側で確認予定。

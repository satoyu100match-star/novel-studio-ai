# プライバシーについて

Novel Studio AIは「作品データは原則ローカル」という方針で作られています
（`CLAUDE.md`「AI機能の原則」参照）。

## 保存されるデータ

- 作品の本文・設定・プロット等はすべて、お使いの端末上のSQLiteデータベース
  ファイル(アプリのデータフォルダ内)にのみ保存されます。外部のサーバーへ
  自動的に送信されることはありません。
- アプリの動作ログはお使いの端末上にファイルとして保存されます（詳細は
  下記「診断ログ」参照）。ログには本文やAPIキーは出力しません。

## AI機能を使う場合

- AI機能(執筆支援・高度分析・作品専用AIチャット等)はすべて任意です。
  設定しなくても他の全機能は通常どおり動作します。
- AI機能を使う場合のみ、選択したデータ（Context Inspectorで事前に
  内容を確認・取捨選択できます）が、あなたが設定したAIプロバイダー
  (Anthropic / OpenAI / Gemini / OpenAI互換)へ送信されます。送信内容は
  常に事前確認を挟み、バイパス経路はありません。
- APIキーはソースコードに埋め込まれておらず、ディスクへ平文保存も
  されません（アプリ実行中のみメモリ上に保持し、再起動のたびに再入力が
  必要です）。ログにも出力されません。

## 診断ログ

- アプリの動作ログは端末上に保存されるのみで、自動的にどこかへ送信される
  ことはありません（いわゆる「テレメトリ」は実装されていません）。
- 不具合報告のためにログを共有したい場合は、「このアプリについて」画面の
  「診断ログを書き出す」から手動でZIPファイルとして書き出せます。
  書き出したファイルをどこに送るか（送るかどうか）は常にあなたの選択です。

## 変更について

このアプリの開発方針が変わり、上記の内容に実質的な変更がある場合は、
このファイルとアプリ内の表示を合わせて更新します。
